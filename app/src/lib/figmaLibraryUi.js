/**
 * Front-facing Figma custom-library / MCP UI (Settings token, Home filter,
 * Push). Keep false until OAuth/allowlisting or a public REST path exists.
 */
export const FIGMA_LIBRARY_UI_ENABLED = false;
